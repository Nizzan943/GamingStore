$(function () {
    /*
    $('form').submit(function (e) {
        e.preventDefault();

        var brands = $('#brands').val();

        $('#toload').load('/Item/SearchByBrand?brands=' + brands);
    });
    */
});